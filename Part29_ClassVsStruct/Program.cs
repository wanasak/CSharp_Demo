using System;

// Classes are a reference type, Structs are a value type
// Classes have a destructor, Structs have no destructor
// Classes can have explicit parameterless constructor, Strucs cannot
// Classes can inherit from another class, Structs cannot
// Both can inherit from an interface

namespace Part29_ClassVsStruct
{
    
    class MainClass
    {
        public static void Main(string[] args)
        {
            
        }
    }
}
